public class CustomPopup : Popup
{
    //code to be added for a custom popup

    public override bool AddMenuText(string text, bool hideOption)
    {
        //do cool custom stuff
        return base.AddMenuText(text, hideOption);
    }
}
